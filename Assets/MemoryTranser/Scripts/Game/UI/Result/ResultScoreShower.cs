using UnityEngine;

namespace MemoryTranser.Scripts.Game.UI.Result {
    public class ResultScoreShower : MonoBehaviour {
        // Start is called before the first frame update
        private void Start() { }

        // Update is called once per frame
        private void Update() { }
    }
}